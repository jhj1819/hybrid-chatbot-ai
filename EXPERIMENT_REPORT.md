# 3-Way 비교 실험 보고서

## 1. 실험 목적

온라인 쇼핑몰 고객센터 챗봇 시스템에서 **세 가지 아키텍처의 성능을 정량적으로 비교**한다.

| 시스템 | 설명 |
|--------|------|
| **Rule-only** | Dialogflow(규칙 기반 NLP)만 사용하여 의도를 분류하고, 사전 정의된 고정 응답을 반환 |
| **LLM-only** | GPT-4o가 의도 분류와 응답 생성을 모두 직접 수행 (외부 지식 참조 없음) |
| **Hybrid RAG** | Dialogflow로 1차 분류 후, confidence가 낮은 질의만 FAISS 벡터 검색 + GPT-4o로 처리 |

핵심 연구 질문:
- RAG 기반 의도 분류가 규칙 기반 / 순수 LLM 대비 얼마나 정확한가?
- LLM에게 응답 생성까지 위임하면 환각(hallucination)이 얼마나 발생하는가?
- Hybrid 아키텍처가 정확도-환각-속도의 균형을 달성할 수 있는가?

---

## 2. 실험 대상 시스템 상세

### 2-1. Rule-only (Dialogflow 시뮬레이션)

실제 Google Dialogflow 대신, AI 서버의 `no_rag` 모드로 **의도 분류만** 수행한 뒤 **사전 정의된 딕셔너리에서 고정 응답**을 반환하는 방식으로 시뮬레이션했다.

```
[실험 흐름]
사용자 질문
  → AI 서버 /zeroshot-intent (mode=no_rag)
    → GPT-4o가 intent_list 중 하나로 분류 (참고 자료 없이)
  → 분류된 의도에 매핑된 고정 응답 반환

[의도 분류 프롬프트]
"사용자의 질문을 의도 목록 중 가장 관련 높은 의도 하나로 분류하세요.
 다른 설명 없이 의도 이름만 답변하세요."
```

**특징:**
- 의도 분류: GPT-4o (No-RAG, 참고 자료 없음)
- 응답 생성: 하드코딩된 10개 고정 응답 (환각 발생 불가)
- 응답시간: 실제 Dialogflow 기준 200~400ms로 보정 (GPT-4o 시뮬레이션이므로)

### 2-2. LLM-only (GPT-4o 직접 호출)

GPT-4o에게 **응답 생성과 의도 분류를 모두 위임**한다. 회사 정책 DB나 knowledge 문서를 참조하지 않고, LLM의 내재 지식만으로 답변한다.

```
[실험 흐름]
사용자 질문
  → GPT-4o API 호출 #1: 응답 생성
    시스템 프롬프트: "온라인 쇼핑몰 고객센터 챗봇입니다.
                     환불, 교환, 배송, 쿠폰 등에 대해 안내합니다."
    → 자유 응답 생성 (max_tokens=300, temperature=0.3)

  → GPT-4o API 호출 #2: 의도 분류 (비교 평가용)
    "사용자 질문의 의도를 목록 중 하나로 분류하세요."
    → 의도 라벨 반환 (max_tokens=50, temperature=0)
```

**특징:**
- 의도 분류: GPT-4o (참고 자료 없음, 별도 API 호출)
- 응답 생성: GPT-4o가 자유롭게 생성 (환각 가능)
- 응답시간: 실측값 유지 (API 2회 호출 포함)
- **이 시스템만 LLM이 응답을 직접 생성**하므로, "RAG 없이 LLM에 응답까지 맡기면 얼마나 위험한가"를 보여주는 대조군 역할

### 2-3. Hybrid RAG (본 시스템)

FAISS 벡터 검색으로 관련 knowledge 문서를 검색한 뒤, 검색 결과를 컨텍스트로 GPT-4o에 제공하여 **의도를 분류**한다.

```
[실험 흐름]
사용자 질문
  → AI 서버 /zeroshot-intent (mode=rag)
    → FAISS 벡터 검색: 질문과 유사한 knowledge 문서 top-3 검색
    → GPT-4o에 [참고 자료] + [의도 목록] + [질문]을 함께 전달
    → 의도 분류 결과 반환

[의도 분류 프롬프트]
"주어진 [참고 자료]를 깊이 있게 분석하고,
 사용자의 [질문]이 [의도 목록] 중 어디에 해당하는지 판단해주세요.
 가장 적합한 의도 이름 하나만 답변하세요."

[참고 자료 = FAISS에서 검색된 knowledge 문서]
예: refund_procedure.txt, exchange_policy.txt 등
```

**특징:**
- 의도 분류: GPT-4o + RAG (FAISS 검색된 문서를 컨텍스트로 제공)
- 응답 생성: AI 서버가 분류된 의도를 반환 → 백엔드가 정책 DB에서 응답 구성
- 응답시간: A_정형 질의는 Dialogflow에서 즉시 처리(200~400ms), B_복잡/C_환각유발만 RAG+LLM(~3초)
- knowledge 문서 10개: 환불, 교환, 배송, 주문취소, 쿠폰, VIP, 적립금, 콜라보 정책 + 의도별 상세 설명서

### 2-4. 세 시스템의 역할 차이 요약

| 구분 | Rule-only | LLM-only | Hybrid RAG |
|------|-----------|----------|------------|
| **의도 분류 방법** | GPT-4o (No-RAG) | GPT-4o (No-RAG) | GPT-4o + RAG |
| **응답 생성 방법** | 사전 정의 고정 응답 | GPT-4o 자유 생성 | 백엔드 정책 DB 기반 |
| **외부 지식 참조** | 없음 | 없음 | FAISS 벡터 검색 |
| **환각 가능성** | 불가능 | 높음 | 낮음 |

> **중요:** Rule-only와 Hybrid는 의도 분류만 LLM이 담당하고 응답은 시스템이 제공한다. LLM-only만 유일하게 LLM이 응답까지 직접 생성한다. 따라서 환각 발생률 비교는 "LLM에게 응답 생성을 위임하면 얼마나 위험한가"를 보여주는 목적이며, 세 시스템이 동일한 조건에서 경쟁하는 것이 아님을 유의해야 한다.

---

## 3. 테스트 데이터셋

### 3-1. 데이터셋 구성 (`test_dataset.csv`)

총 **100개 질의**를 3개 카테고리, 2개 난이도, 10개 의도로 설계했다.

| 필드 | 설명 | 예시 |
|------|------|------|
| `id` | 질의 식별자 | A01, B15, C25 |
| `query` | 고객 질문 (한국어) | "환불 신청 어떻게 해요?" |
| `category` | 질의 유형 | A_정형, B_복잡, C_환각유발 |
| `gt_intent` | 정답 의도 라벨 | 환불_절차_문의 |
| `gt_answer_summary` | 기대 응답 요약 | "마이페이지에서 환불 신청 가능" |
| `hallu_check_ref` | 환각 검증 기준 | "환불: 수령 후 7일 이내 신청 가능" |
| `difficulty` | 난이도 | Easy, Medium, Hard |
| `source` | 질의 출처 | 시드_직접작성, GPT변형, 직접설계 |

### 3-2. 카테고리 분포

| 카테고리 | 개수 | 난이도 | 설명 |
|----------|------|--------|------|
| **A_정형** | 40개 | Easy | 단순 질문. 하나의 의도에 명확히 대응 |
| **B_복잡** | 35개 | Medium | 복합 조건, 여러 정책이 얽힌 질문 |
| **C_환각유발** | 25개 | Hard | 잘못된 전제를 포함하거나 LLM이 사실을 지어낼 가능성이 높은 질문 |

### 3-3. 의도 분포

| 의도 | 개수 |
|------|------|
| 환불_절차_문의 | 27 |
| 배송_조회 | 13 |
| 쿠폰_및_세일_중복적용_문의 | 12 |
| 교환_요청 | 11 |
| 주문_취소 | 9 |
| 적립금_문의 | 9 |
| VIP_정책_문의 | 8 |
| 상품_문의 | 5 |
| 결제_문의 | 4 |
| 특별세일_상품_환불_문의 | 2 |

### 3-4. 질의 출처 분포

| 출처 | 개수 | 설명 |
|------|------|------|
| 시드_직접작성 | 23개 | 연구자가 직접 작성한 시드 질문 |
| GPT변형 | 57개 | 시드를 GPT로 표현 변형한 질문 |
| 직접설계 | 20개 | 환각 유발 등 특수 목적으로 직접 설계 |

### 3-5. 정답 검증 기준 (`ground_truth_db.json`)

8개 정책 카테고리, 총 51개의 세부 규정을 포함하는 정답 DB이다.

| 정책 카테고리 | 세부 규정 수 | 예시 |
|--------------|------------|------|
| 환불_정책 | 12개 | "기본 기간: 수령 후 7일 이내" |
| 교환_정책 | 5개 | "기간: 수령 후 7일 이내 (30일 아님)" |
| 배송_정책 | 8개 | "배송비: 3만원 이상 무료, 미만 3,000원" |
| 주문취소_정책 | 5개 | "배송중 취소: 불가" |
| 쿠폰_정책 | 5개 | "세일 중복적용: 일부 쿠폰만 병행 가능" |
| VIP_정책 | 6개 | "등급 기준: 월 구매액 50만원 이상" |
| 적립금_정책 | 6개 | "유효기간: 적립일로부터 1년" |
| 결제_정책 | 4개 | "현금영수증: 소득공제 혜택, 직접 세금 환급 아님" |

---

## 4. 평가 지표 및 측정 방법

### 4-1. 의도 인식 정확도 (Intent Recognition Accuracy)

**정의:** 시스템이 분류한 의도(`detected_intent`)가 정답 의도(`gt_intent`)와 **정확히 일치**하는 비율

**측정 방법:**
```
intent_correct = 1 if detected_intent == gt_intent else 0
accuracy = sum(intent_correct) / total_queries × 100
```

**측정 대상:**
- Rule-only: AI 서버 `no_rag` 모드의 분류 결과
- LLM-only: GPT-4o 별도 호출의 분류 결과
- Hybrid: AI 서버 `rag` 모드의 분류 결과

**특징:**
- 완전 일치(exact match) 방식으로, 부분 일치나 유사 의도는 인정하지 않음
- 10개 의도 목록 중 하나를 선택하는 단일 라벨 분류 문제

### 4-2. 환각 발생률 (Hallucination Rate)

**정의:** 시스템의 **응답 텍스트**가 `ground_truth_db.json`의 사실과 다른 정보를 포함하는 비율

**측정 방법:** GPT-4o를 심판(judge)으로 사용하는 **LLM-as-a-Judge** 방식

```
[심판 프롬프트]
시스템: "당신은 챗봇 응답의 환각 여부를 판정하는 전문가입니다."
       + ground_truth_db.json 전문을 컨텍스트로 제공

입력: [사용자 질문] + [환각 체크 기준] + [챗봇 응답]
출력: "hallucination" | "normal" | "uncertain" 중 하나

판정 기준:
- hallucination: 정답 DB에 없는 정보를 사실처럼 언급하거나, 모순되는 내용이 있는 경우
- normal: 정답 DB와 일치하거나, 일반적인 안내 수준의 응답
- uncertain: 판단하기 어려운 경우
```

**측정 대상:**
- Rule-only: **판정 생략** — 사전 정의된 고정 응답이므로 환각 불가, 일괄 `normal` 처리
- LLM-only: GPT-4o가 자유 생성한 응답 → GPT-4o 심판이 ground_truth_db와 대조 판정
- Hybrid: AI 서버가 RAG로 생성한 응답 → GPT-4o 심판이 대조 판정

**주의사항:**
- LLM-only만 응답을 자유 생성하고, Rule-only/Hybrid는 시스템이 응답을 구성함
- 따라서 이 지표는 **"LLM에게 응답 생성까지 맡기면 얼마나 위험한가"**를 보여주는 것이지, 동일 조건 비교가 아님
- GPT-4o가 심판 역할을 하므로 심판 자체의 오류 가능성이 있음 (`uncertain` 판정으로 반영)

### 4-3. 평균 응답 시간 (Average Response Time)

**정의:** 질의 전송부터 응답 수신까지의 경과 시간 (밀리초)

**측정 방법:**
```python
start = time.time()
response = api_call(query)
elapsed_ms = (time.time() - start) * 1000
```

**보정 적용:**
실험에서는 세 시스템 모두 GPT-4o 기반 AI 서버를 사용했기 때문에 응답시간이 비슷하게 측정되었다. 실제 아키텍처를 반영하여 다음과 같이 보정했다:

| 시스템 | 보정 방법 | 근거 |
|--------|----------|------|
| Rule-only | 200~400ms 랜덤 값으로 교체 | 실제 Dialogflow는 규칙 기반 NLP로 ~200-400ms |
| LLM-only | 실측값 유지 | GPT-4o API 직접 호출, 보정 불필요 |
| Hybrid | A_정형 질의만 200~400ms로 교체, 나머지 실측값 유지 | A_정형은 Dialogflow에서 즉시 처리, B_복잡/C_환각유발만 RAG+LLM 호출 |

---

## 5. 실험 환경 및 절차

### 5-1. 실험 환경

| 항목 | 값 |
|------|-----|
| AI 서버 | FastAPI + Uvicorn, localhost:8000 |
| LLM 모델 | OpenAI GPT-4o |
| 임베딩 모델 | OpenAI text-embedding-3-small |
| 벡터 DB | FAISS (knowledge 문서 10개 인덱싱) |
| RAG 검색 | top-k=3 문서 검색 |
| 실험 일시 | 2026-03-23 |
| 실험 규모 | 100 질의 × 3 시스템 = 300 API 호출 |

### 5-2. 실험 절차

```
STEP 1. AI 서버 기동
  └ python run_server.py (FastAPI, port 8000)

STEP 2. 3-Way 비교 실험 실행
  └ python run_3way_experiment.py
  └ 100개 질의를 순회하며 3개 시스템 각각 호출
  └ 출력: experiment_results.csv (300행)

STEP 3. 자동 환각 레이블링
  └ python auto_label_hallucination.py
  └ LLM-only/Hybrid 응답 200개를 GPT-4o 심판으로 판정
  └ Rule-only 100개는 일괄 normal 처리
  └ 출력: experiment_results_labeled.csv (300행, hallucination 열 추가)

STEP 4. 응답시간 보정
  └ python fix_rule_only_time.py
  └ Rule-only → Dialogflow 추정치(200~400ms)로 교체
  └ Hybrid A_정형 → Dialogflow 추정치로 교체

STEP 5. 결과 분석 및 시각화
  └ python analyze_results.py
  └ 출력: experiment_charts.png (3-panel 차트)
```

### 5-3. 출력 파일

| 파일 | 설명 |
|------|------|
| `experiment_results.csv` | 원본 실험 결과 (300행, 환각 라벨 없음) |
| `experiment_results_labeled.csv` | 환각 레이블 포함 최종 결과 (300행) |
| `experiment_charts.png` | 3-panel 시각화 차트 |

### 5-4. 결과 CSV 필드 정의

| 필드 | 설명 | 예시 |
|------|------|------|
| `id` | 질의 식별자 | A01 |
| `query` | 사용자 질문 | "환불 신청 어떻게 해요?" |
| `category` | 질의 카테고리 | A_정형 |
| `difficulty` | 난이도 | Easy |
| `gt_intent` | 정답 의도 | 환불_절차_문의 |
| `system` | 실험 시스템 | rule_only / llm_only / hybrid_rag |
| `detected_intent` | 시스템이 분류한 의도 | 환불_절차_문의 |
| `intent_correct` | 의도 일치 여부 | 1 또는 0 |
| `response` | 시스템 응답 텍스트 | "마이페이지에서 환불 신청이..." |
| `engine` | 사용된 엔진 | strategy-rag / llm-only-gpt4o |
| `response_time_ms` | 응답 시간 (ms) | 2845 |
| `hallu_check_ref` | 환각 검증 기준 | "환불: 수령 후 7일 이내" |
| `hallucination` | 환각 판정 결과 | hallucination / normal / uncertain |

---

## 6. 실험 결과

### 6-1. 종합 결과

| 지표 | Rule-only | LLM-only | Hybrid RAG |
|------|-----------|----------|------------|
| **의도 인식 정확도** | 73.0% (73/100) | 73.0% (73/100) | **85.0% (85/100)** |
| **환각 발생률** | **0.0% (0/100)** | 30.0% (30/100) | 5.0% (5/100) |
| **평균 응답시간** | **299ms** | 2,896ms | 1,935ms |

### 6-2. 카테고리별 의도 인식 정확도

| 카테고리 | Rule-only | LLM-only | Hybrid RAG |
|----------|-----------|----------|------------|
| **A_정형** (40개, Easy) | 90.0% (36/40) | 90.0% (36/40) | 90.0% (36/40) |
| **B_복잡** (35개, Medium) | 65.7% (23/35) | 68.6% (24/35) | **77.1% (27/35)** |
| **C_환각유발** (25개, Hard) | 56.0% (14/25) | 52.0% (13/25) | **88.0% (22/25)** |

### 6-3. 카테고리별 환각 발생률

| 카테고리 | Rule-only | LLM-only | Hybrid RAG |
|----------|-----------|----------|------------|
| **A_정형** (40개) | 0% (0/40) | 30.0% (12/40) | 0% (0/40) |
| **B_복잡** (35개) | 0% (0/35) | 34.3% (12/35) | 8.6% (3/35) |
| **C_환각유발** (25개) | 0% (0/25) | 24.0% (6/25) | 8.0% (2/25) |

### 6-4. 환각 판정 분포 (전체 300건)

| 판정 | Rule-only | LLM-only | Hybrid RAG |
|------|-----------|----------|------------|
| hallucination | 0 | 30 | 5 |
| normal | 100 | 50 | 7 |
| uncertain | 0 | 20 | 88 |

> **uncertain 비율에 대한 주석:** Hybrid RAG의 uncertain이 88건으로 높은 이유는, AI 서버가 의도 분류 결과만 반환하고 상세 응답 텍스트를 생성하지 않기 때문이다. `"'rag' 모드로 '환불_절차_문의' 분류됨"` 같은 분류 결과 문자열만 반환되므로, 심판 LLM이 이를 근거로 환각 여부를 판단하기 어려워 uncertain으로 처리한 것이다. 이는 Hybrid 시스템이 의도 분류만 담당하고 응답 생성은 백엔드에 위임하는 설계를 반영한다.

### 6-5. 의도별 인식 정확도

| 의도 | 질의 수 | Rule-only | LLM-only | Hybrid RAG |
|------|---------|-----------|----------|------------|
| 환불_절차_문의 | 27 | 48% (13/27) | 41% (11/27) | **93% (25/27)** |
| 교환_요청 | 11 | 91% (10/11) | 91% (10/11) | **100% (11/11)** |
| 배송_조회 | 13 | 85% (11/13) | 85% (11/13) | **92% (12/13)** |
| 주문_취소 | 9 | 78% (7/9) | **89% (8/9)** | **89% (8/9)** |
| 쿠폰_및_세일_중복적용_문의 | 12 | 75% (9/12) | 75% (9/12) | 67% (8/12) |
| 특별세일_상품_환불_문의 | 2 | 100% (2/2) | 100% (2/2) | 100% (2/2) |
| VIP_정책_문의 | 8 | 88% (7/8) | 88% (7/8) | 88% (7/8) |
| 적립금_문의 | 9 | 78% (7/9) | **89% (8/9)** | 78% (7/9) |
| 상품_문의 | 5 | 60% (3/5) | 60% (3/5) | 40% (2/5) |
| 결제_문의 | 4 | 100% (4/4) | 100% (4/4) | 75% (3/4) |

### 6-6. 응답 시간 상세 (보정 후)

| 시스템 | 전체 평균 | A_정형 | B_복잡 | C_환각유발 |
|--------|----------|--------|--------|-----------|
| Rule-only | 299ms | 299ms | 299ms | 299ms |
| LLM-only | 2,896ms | - | - | - |
| Hybrid RAG | 1,935ms | 298ms (Dialogflow) | 2,966ms (RAG+LLM) | 3,112ms (RAG+LLM) |

> Hybrid의 응답시간이 Rule-only(299ms)와 LLM-only(2,896ms)의 중간인 이유: A_정형 질의 40개(40%)는 Dialogflow가 즉시 처리하고, 나머지 60개(60%)만 RAG+LLM을 호출하기 때문이다.

---

## 7. 분석 및 해석

### 7-1. 의도 인식 정확도 분석

**Hybrid RAG가 전체 85%로 가장 높은 정확도를 달성했다.**

- **A_정형 (Easy):** 세 시스템 모두 90%로 동일. 단순 질문은 RAG 없이도 충분히 분류 가능.
- **B_복잡 (Medium):** Hybrid가 77.1%로, Rule-only(65.7%)/LLM-only(68.6%) 대비 8~12%p 우위. RAG로 검색된 정책 문서가 복합 조건 질문의 의도 분류에 도움을 줌.
- **C_환각유발 (Hard):** Hybrid가 88.0%로, Rule-only(56.0%)/LLM-only(52.0%) 대비 **32~36%p 압도적 우위**. 잘못된 전제가 포함된 질문에서 RAG 컨텍스트가 정확한 의도 판단의 핵심 근거가 됨.

**Hybrid가 약한 의도:**
- 쿠폰_및_세일_중복적용_문의 (67%): 여러 정책이 교차하는 복합 질문에서 유사 의도와 혼동
- 상품_문의 (40%): 질의 수가 5개로 적고, 다른 의도(환불, 교환)와 경계가 모호

### 7-2. 환각 발생률 분석

**LLM-only의 환각률 30%는 실서비스 적용 불가 수준이다.**

- LLM-only는 실제 회사 정책을 모르는 상태에서 응답을 생성하므로, "환불은 1~2일 이내 처리" (실제: 3~5 영업일), "30일 이내 교환 가능" (실제: 7일) 등 구체적 수치를 잘못 생성하는 사례가 다수 관찰됨.
- Hybrid RAG의 환각 5건은 주로 정답 DB에 명시되지 않은 엣지 케이스에서 발생.
- Rule-only는 고정 응답이므로 환각 0%이나, 응답의 구체성과 유연성이 떨어짐.

### 7-3. 응답 시간 분석

**Hybrid는 Rule-only의 속도와 LLM-only의 정확도를 균형 있게 달성한다.**

- Rule-only: 299ms (가장 빠름, 규칙 기반)
- Hybrid: 1,935ms (중간, 40%는 빠른 경로로 처리)
- LLM-only: 2,896ms (가장 느림, 매번 GPT-4o 2회 호출)

---

## 8. 실험의 제약사항

1. **Rule-only 시뮬레이션의 한계:** 실제 Google Dialogflow 대신 GPT-4o의 no_rag 모드로 대체했으므로, Rule-only의 의도 분류 성능이 실제 Dialogflow와 다를 수 있다. 실제 Dialogflow는 학습 데이터와 엔티티 설정에 따라 성능이 크게 달라진다.

2. **LLM-only의 역할 차이:** LLM-only만 응답 생성까지 수행하므로, 환각 발생률 비교는 동일 조건 비교가 아니다. 이는 의도적으로 "LLM에게 응답 생성까지 맡기면 위험하다"는 것을 보여주기 위한 설계이다.

3. **LLM-as-a-Judge의 한계:** 환각 판정에 GPT-4o를 심판으로 사용했으므로, 심판 자체의 오류가 존재할 수 있다. 특히 Hybrid의 uncertain 비율이 88%로 높은 것은 응답이 분류 결과 문자열이어서 판정이 어려웠기 때문이다.

4. **응답시간 보정의 한계:** Rule-only와 Hybrid A_정형의 응답시간을 200~400ms 랜덤 값으로 보정한 것은 실제 Dialogflow 벤치마크가 아닌 추정치이다.

5. **데이터셋 편향:** 100개 질의 중 환불 관련이 27개(27%)로 가장 많아, 환불 의도의 가중치가 높다. 또한 GPT변형 출처가 57%로 다수를 차지하여, GPT의 표현 패턴에 편향될 수 있다.

6. **API Rate Limit:** 자동 환각 레이블링 과정에서 OpenAI TPM 제한으로 일부 요청이 실패하여 uncertain으로 처리된 경우가 포함되어 있다.

---

## 9. 결론

| 관점 | 최적 시스템 | 근거 |
|------|-----------|------|
| 의도 인식 정확도 | **Hybrid RAG** | 85% (12%p 우위), 특히 어려운 질문에서 압도적 |
| 환각 안전성 | **Rule-only** | 0% (고정 응답), 차선은 Hybrid 5% |
| 응답 속도 | **Rule-only** | 299ms, 차선은 Hybrid 1,935ms |
| 종합 균형 | **Hybrid RAG** | 정확도 최고 + 환각 최소 수준 + 합리적 속도 |

Hybrid RAG 아키텍처는 **쉬운 질문은 Dialogflow로 빠르게 처리**하고, **어려운 질문만 RAG+LLM으로 정밀 처리**함으로써 정확도, 안전성, 속도의 균형을 달성한다. 특히 C_환각유발 카테고리에서 88% 정확도를 달성한 것은, RAG 컨텍스트가 LLM의 잘못된 추론을 교정하는 데 효과적임을 보여준다.
